<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recognize_wave_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:alpha="0"
    android:background="#CC000000"
    android:visibility="gone"
    tools:alpha="1"
    tools:visibility="visible">

    <com.iflytek.cyber.iot.show.core.widget.RecognizeWaveView
        android:id="@+id/tts_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ScrollView
        android:id="@+id/scroll_view"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_70"
        android:layout_gravity="bottom"
        android:layout_marginBottom="@dimen/dp_32"
        android:clipToPadding="false"
        android:paddingStart="@dimen/dp_46"
        android:paddingEnd="@dimen/dp_40">

        <com.iflytek.cyber.iot.show.core.widget.HighlightTextView
            android:id="@+id/tts_text"
            style="@style/TemplateBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="@dimen/sp_20"
            app:highlightColor="@android:color/white"
            app:lineHeight="@dimen/dp_32"
            app:normalColor="@android:color/white"
            tools:text="床前明月光，疑是地上霜。举头望明月，低头思故乡。床前明月光，疑是地上霜。举头望明月，低头思故乡。" />

    </ScrollView>

</FrameLayout>